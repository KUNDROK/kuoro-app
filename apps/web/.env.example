# En desarrollo local, déjalo vacío: Vite proxy envía /api al backend (vite.config.ts).
# En Vercel (staging/producción), URL pública del API en Railway, con sufijo /api/v1:
# VITE_API_BASE_URL=https://api-staging.railway.app/api/v1
VITE_API_BASE_URL=
