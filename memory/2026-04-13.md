Sesion del 2026-04-13

- Se continuo el trabajo en `/asambleas`, siguiendo el ultimo punto del 2026-04-12: mejorar el flujo editable inline de 7 pasos sin mover la preparacion a otra pagina.
- Verificacion inicial:
  - `npm --workspace @ph/web run typecheck` paso correctamente.
  - API y frontend no estaban arriba; se levantaron con `Start-Process`.
  - `http://127.0.0.1:4000/health` respondio `200`.
  - `http://127.0.0.1:5173/asambleas` respondio `200`.
- Cambios en `apps/web/src/pages/AssembliesPage.tsx`:
  - Se agrego `getRecipientStatusLabel()` para mostrar estados de destinatarios en lenguaje claro.
  - Paso 1 ahora expone mas campos editables inline: estado, servicio de conferencia, segunda convocatoria y notas internas.
  - Paso 2 ahora permite editar descripcion del punto y marcar si requiere anexo.
  - Paso 5 ahora muestra hasta 4 destinatarios, agrega estado legible y nota por destinatario, y muestra estado vacio si aun no hay destinatarios listos.
  - Paso 6 ahora permite editar reglas adicionales de acceso: documento obligatorio, acompanantes, un votante activo por unidad y validacion manual de respaldo.
  - Revision final ahora muestra tambien accesos listos y segunda convocatoria.
- Cambios en `apps/web/src/styles.css`:
  - Se agrego soporte visual para `textarea` en el editor de pasos.
  - Se agrego `assemblies-editor-wide`, mejor ancho de filas editoras, `small` dentro de filas, y estado vacio `assemblies-empty-step`.
  - Se mantuvo el boton de guardado fijo abajo a la derecha del panel del paso.
- Verificaciones despues de editar:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion - paso 01 de preparacion:

- El usuario pidio trabajar los 7 pasos uno por uno, empezando por el paso 01.
- Problemas indicados:
  - `Tipo` y `Modalidad` permitian combinaciones incoherentes, por ejemplo `Tipo: Mixta` y `Modalidad: Virtual`.
  - Si la modalidad es virtual, los campos deben ser de reunion virtual; si es presencial, no deben aparecer campos virtuales.
  - El selector `datetime-local` era incomodo porque habia que hacer click fuera para salir.
  - Quitar `Permite segunda convocatoria`; si el administrador necesita segunda convocatoria, debe crearla desde el `Tipo: Segunda convocatoria`.
- Cambios hechos:
  - En `AssembliesPage.tsx`, `Mixta` se quito de `Tipo` y queda solo como `Modalidad`.
  - Se agregaron opciones tipadas para `Tipo`, `Modalidad` y servicios virtuales.
  - Se agrego normalizacion del formulario:
    - datos viejos con `type: mixta` pasan a `ordinaria` y conservan `modality: mixta` si aplica.
    - `no_presencial` y `comunicacion_escrita` fuerzan modalidad virtual.
    - `derecho_propio` fuerza modalidad presencial.
    - modalidad presencial limpia enlace virtual y usa `conferenceService: ninguno`.
    - modalidad virtual limpia lugar fisico y usa servicio virtual.
    - `allowsSecondCall` siempre queda `false` y `secondCallScheduledAt` vacio.
  - En `/asambleas`, fecha y hora se separaron en inputs `date` y `time`.
  - En `/asamblea/preparacion?step=0`, se aplico la misma coherencia: se quito `Tipo: Mixta`, se removio el bloque de segunda convocatoria, se separo fecha/hora y se oculta el servicio virtual si no aplica.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:5173/asamblea/preparacion?step=0` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion - campos informativos del paso 01:

- El usuario aclaro que algunos campos no deben ser llenados por el administrador:
  - `Enlace de reunion`: debe generarlo automaticamente el proveedor embebido cuando se implemente. El administrador solo lo ve/copia si necesita envio manual.
  - `Estado`: lo determina el sistema segun el avance de la asamblea.
  - `Servicio virtual`: depende del proveedor adoptado; solo se muestra.
- Cambios hechos:
  - En `AssembliesPage.tsx`, `Enlace de reunion`, `Estado` y `Servicio virtual` pasaron de inputs/selects a campos informativos.
  - En `AssemblyPreparationPage.tsx`, se aplico la misma regla para mantener coherencia con `/asambleas`.
  - Se agrego estilo `.assemblies-system-field` para que estos valores se lean como datos de sistema y no como campos editables.
  - En `apps/api/src/routes.ts`, la validacion de configuracion de asamblea dejo de exigir `virtualAccessUrl` cuando la modalidad es virtual o mixta, porque el enlace debe venir de la futura integracion de proveedor.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/api run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:5173/asamblea/preparacion?step=0` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion - reordenamiento conceptual de pasos 2 y 3:

- El usuario aclaro que `Orden del dia` y `Documentos/anexos` deben ser un solo modulo: `Presentacion de la asamblea`.
- Intencion de producto:
  - El administrador construye una presentacion guiada para la reunion.
  - Cada punto del orden del dia funciona como bloque de presentacion para seguir cronologicamente en la transmision.
  - La descripcion debe ser contenido amplio para mostrar durante la transmision, no un input pequeno.
  - Los anexos pertenecen al punto de presentacion donde se usan.
  - Los momentos de votacion se preparan dentro de esos puntos.
- El usuario tambien aclaro que `Representacion y poderes` no es un proceso de esta cabina:
  - Los poderes/notificaciones se gestionan desde `Comunicaciones`, `Reportes` y `Unidades`.
  - En preparacion solo deben aparecer como alertas de revision final.
- Cambios hechos:
  - En `AssembliesPage.tsx`:
    - Los 7 pasos quedaron como:
      1. Tipo y datos base de la asamblea
      2. Presentacion de la asamblea
      3. Convocatoria y destinatarios
      4. Acceso e identidad
      5. Revision final de alistamiento
      6. Sala en vivo
      7. Cierre e historico
    - El paso 02 guarda agenda y documentos juntos.
    - El campo de descripcion de punto paso a `textarea` y se renombro a contenido para la diapositiva/transmision.
    - Se agrego una vista previa simple de presentacion.
    - Revision final muestra pendientes de poderes/unidades como alertas y enlaza hacia Comunicaciones y Reportes.
    - Los pasos Sala en vivo y Cierre e historico quedaron como destinos operativos/informativos sin boton de guardar.
  - En `AssemblyPreparationPage.tsx`:
    - Se actualizo la lista de pasos con el mismo flujo.
    - `Orden del dia` y `Anexos` se muestran juntos en el paso 02.
    - Convocatoria, Acceso y Revision final se desplazaron a los nuevos indices.
    - Se agregaron secciones preparadas para Sala en vivo y Cierre e historico.
    - Se cambio el texto de revision final para aclarar que poderes se atienden fuera de esta cabina.
  - En `AdminDashboardPage.tsx`:
    - El checklist del dashboard ahora usa `Presentacion de la asamblea`.
    - Se eliminaron como pasos separados `Documentos y anexos` y `Representacion y poderes`.
    - Se agregaron `Sala en vivo` y `Cierre e historico`.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `npm --workspace @ph/api run typecheck`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:5173/asamblea/preparacion?step=1` respondio `200`
  - `http://127.0.0.1:5173/asamblea/preparacion?step=5` respondio `200`
  - `http://127.0.0.1:5173/dashboard` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion - eliminar paso de Convocatoria:

- El usuario indico que `Convocatoria y destinatarios` no debe ser un paso dentro de preparacion porque no tiene sentido mostrar solo algunos destinatarios.
- Cambios hechos:
  - En `AssembliesPage.tsx`, se elimino `Convocatoria y destinatarios` del flujo visible.
  - El flujo visible quedo en 6 pasos:
    1. Tipo y datos base
    2. Presentacion de la asamblea
    3. Acceso e identidad
    4. Revision final
    5. Sala en vivo
    6. Cierre e historico
  - Se removio el editor inline de destinatarios/convocatoria de `/asambleas`.
  - En `AssemblyPreparationPage.tsx`, se elimino `Convocatoria` de la lista de pasos y se remapearon Acceso, Revision final, Sala y Cierre.
  - En `AdminDashboardPage.tsx`, se elimino `Convocatoria y destinatarios` del checklist.
  - La convocatoria puede seguir apareciendo como metrica/alerta de sistema en revision final, pero no como paso propio.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `npm --workspace @ph/api run typecheck`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:5173/asamblea/preparacion?step=2` respondio `200`
  - `http://127.0.0.1:5173/dashboard` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion - paso 02 como presentacion asistida:

- El usuario aclaro que el paso 02 debe funcionar como estructura de presentacion asistida por IA para que durante la asamblea se inicie una presentacion con los puntos creados por el administrador.
- Cambios hechos:
  - En `AssembliesPage.tsx`:
    - Se agrego un bloque `Asistente IA` dentro del paso 02.
    - Por ahora la asistencia es local/pre-IA: boton `Generar estructura sugerida` crea un guion editable con apertura, aprobacion de orden del dia, informes, decisiones sometidas a votacion y cierre.
    - Se agrego selector lateral de puntos/diapositivas.
    - El editor del punto seleccionado ahora tiene titulo, tipo, regla de votacion, contenido amplio de diapositiva y si requiere anexo.
    - La vista previa muestra el punto seleccionado y marca si tiene momento de votacion.
  - En `packages/contracts/src/index.ts`:
    - `AssemblyOverview.agenda` ahora incluye `description`, `type`, `votingRule`, `requiresAttachment` y `order`.
  - En `apps/api/src/domain/assembly.ts` y `apps/api/src/routes.ts`:
    - La sala de asamblea ya no usa solo agenda demo fija.
    - `GET /assemblies/:assemblyId` carga los puntos guardados con `listAgendaItemsByAssembly(assemblyId)` y los entrega a la sala.
  - En `AssemblyRoomPage.tsx`:
    - La sala muestra `Presentacion en vivo`, el titulo y el contenido del punto activo.
    - El boton principal cambia segun si el punto tiene regla de votacion.
    - La lista inferior ahora muestra la presentacion con descripcion y regla de votacion.
- Nota de producto:
  - Aun no hay llamada real a un modelo de IA. Quedo montada una asistencia local como base para sustituirla luego por generacion con IA real.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/api run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:5173/asamblea/preparacion?step=1` respondio `200`
  - `http://127.0.0.1:5173/asamblea/asm-demo` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion - generador real de diapositivas:

- El usuario aclaro que no basta con agenda asistida: quiere un generador de presentaciones para que el administrador comparta diapositivas visibles a propietarios.
- Cambio conceptual aplicado:
  - Se separo el punto que el administrador quiere tratar de la diapositiva que vera el propietario.
  - Cada punto puede tener:
    - `description`: intencion/contenido base del administrador.
    - `slideTitle`: titulo de la diapositiva.
    - `slideContent`: texto proyectado a propietarios.
    - `speakerNotes`: notas para el administrador/moderador.
    - `votePrompt`: pregunta de votacion, si aplica.
- Cambios tecnicos:
  - `packages/contracts/src/index.ts`: `AgendaItemInput`, `AgendaItemSummary` y `AssemblyOverview.agenda` ahora soportan campos de diapositiva.
  - `apps/api/src/db.ts`: `StoredAgendaItem` soporta campos de diapositiva.
  - `apps/api/src/routes.ts`: guardado y entrega de agenda preserva campos de diapositiva.
  - `apps/api/src/domain/assembly.ts`: agenda demo de fallback tambien tiene campos de diapositiva.
  - `AssembliesPage.tsx`: el paso 02 ahora tiene boton para generar una presentacion base y boton para generar la diapositiva del punto seleccionado.
  - `AssemblyRoomPage.tsx`: la sala muestra `slideTitle`, `slideContent`, notas del administrador y pregunta de votacion cuando existen.
- Nota:
  - La generacion sigue siendo local/pre-IA, pero ya opera con el modelo mental correcto de diapositivas. El siguiente paso natural es reemplazar esa funcion local por llamada a IA real.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/api run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:5173/asamblea/asm-demo` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion - alinear la cabina legacy con diapositivas:

- Se aplico el mismo modelo de generador de diapositivas en `AssemblyPreparationPage.tsx`, no solo en `/asambleas`.
- El paso 02 ahora usa selector de diapositivas, generacion local de presentacion base, generacion de diapositiva actual, vista previa de transmision, notas para administrador y pregunta de votacion.
- Se ajustaron textos visibles que seguian hablando de `orden del dia` para que hablen de `presentacion` o `diapositiva`.
- Se actualizo la agenda demo de fallback para usar `Ruta de la asamblea` en vez de `Aprobacion del orden del dia`.
- Verificaciones adicionales:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/api run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:5173/asamblea/preparacion?step=1` respondio `200`
  - `http://127.0.0.1:5173/asamblea/asm-demo` respondio `200`

Continuacion - documentacion y handoff:

- El usuario pidio guardar cambios y dejar instrucciones para que otra IA pueda retomar el proyecto.
- Se creo `docs/AI_HANDOFF.md` como guia de continuidad con contexto de producto, arquitectura actual, archivos clave, decisiones del paso 01 y del generador de diapositivas del paso 02.
- Se actualizo `README.md` para apuntar al handoff y reflejar que el foco actual es el generador de presentaciones.
- Se actualizo `ARCHITECTURE.md` con el estado real del repo: monorepo npm, web React/Vite, API HTTP TypeScript nativa, contratos compartidos y persistencia JSON demo con Prisma en transicion.
- Se reemplazo `docs/NEXT_STEPS.md` con prioridades actuales: IA real para diapositivas, control de diapositiva activa en sala, persistencia/tiempo real y mantener convocatoria/poderes fuera de la cabina editable.
- Se actualizo `MEMORY.md` con las decisiones vigentes de 6 pasos y presentacion por diapositivas.
