Sesion del 2026-04-12

- Se continuo el trabajo sobre el dashboard principal de Quorum en `http://127.0.0.1:5173/dashboard`.
- El usuario dijo que le gusta como va quedando el dashboard, pero le preocupa la optimizacion y no quiere sacrificar secciones. Decision: optimizar sin quitar contenido.
- Se arreglo acceso local varias veces:
  - API confirmada en `http://127.0.0.1:4000/health`.
  - Frontend en `http://127.0.0.1:5173/dashboard`.
  - Cuando Vite fallo por `spawn EPERM` de esbuild, se levanto con permisos elevados.

Cambios principales de UX en dashboard:

- Topbar:
  - Se quito la barra de busqueda superior.
  - El toggle claro/oscuro dejo de ser una pastilla ovalada con texto y paso a boton compacto de icono con `aria-label` y `title`.

- Acciones siguientes:
  - El contador `2 abiertas` se convirtio en badge de una sola linea.
  - La lista muestra maximo 3 acciones visibles y agrega `+N acciones mas` si hay mas.

- Preparacion de la asamblea:
  - Se reemplazo la grilla de tarjetas por un checklist/timeline.
  - Se redefinieron los pasos para que correspondan a la preparacion real de la asamblea, no a prerrequisitos generales:
    1. Tipo y datos base de la asamblea
    2. Orden del dia estructurado
    3. Documentos y anexos
    4. Representacion y poderes
    5. Convocatoria y destinatarios
    6. Acceso e identidad
    7. Revision final de alistamiento
  - Los marcadores son siempre numeros; ya no se mezcla check con numero.
  - Solo hay dos estados visuales claros: hecho y pendiente.
  - Cada paso del checklist ahora es clicable y dirige a `/asamblea/preparacion?step=N`.
  - `AssemblyPreparationPage` ahora lee `?step=N` con `useSearchParams` y abre directamente el paso correspondiente.
  - Se agrego un bloque de cronometro/cuenta regresiva a la derecha de la seccion, alimentado por `assembly.scheduledAt`.
  - El cronometro se aislo en un componente `AssemblyCountdown` para que la actualizacion cada segundo no fuerce el render de todo el dashboard.

- Unidades con atencion requerida:
  - Las unidades ahora muestran etiqueta clara usando `formatUnitLabel`, por ejemplo `Torre 5 Apartamento 301`, en vez de solo `5 301`.
  - Cada fila de unidad ahora enlaza a `/unidades/:unitId?propertyId=...`.

- Alertas criticas:
  - Se reforzo visualmente la seccion con icono, borde lateral y tonos de alerta mas claros.
  - La alerta de unidades sin votante ya no usa un filtro generico por status, porque eso producia discrepancia:
    - Dashboard contaba `no_voter` desde `assemblyReadiness`.
    - Lista de unidades calculaba salud de unidad con otra logica y mostraba mas registros.
  - Solucion: la alerta ahora pasa `unitIds` exactos por URL:
    - `/unidades?propertyId=...&unitIds=id1,id2,id3&filterLabel=sin votante habilitado`
  - `UnitsListPage` ahora soporta `unitIds` y `filterLabel` para mostrar exactamente las unidades que originaron la alerta.

- Accesos directos inferiores:
  - Se redisenaron como previews/accesos directos mas consistentes para Copropiedad, Comunicaciones, Asambleas y Reportes.
  - Cada preview tiene icono, categoria, titulo, resumen, dos metricas y CTA.
  - Importante: el usuario pidio despues "zona de los sitios web" refiriendose al footer, no a un modulo de Sitios web. Se deshizo la interpretacion equivocada:
    - Se elimino la ruta/pagina `/sitios-web`.
    - Se elimino el icono `globe`.
    - Se elimino el modulo preview "Sitios web".
  - Se agrego un footer real al final del dashboard con:
    - Quorum
    - descripcion breve
    - datos de desarrollador/equipo
    - soporte placeholder `soporte@quorum.local`
    - version `0.1.0`

Optimizaciones hechas:

- Se aislo el cronometro en `AssemblyCountdown` para que el tick de 1 segundo no renderice todo el dashboard.
- Se agrego `toProgressWidth()` para normalizar porcentajes de barras y evitar valores fuera de rango o `NaN`.
- Se verifico que no se quitaran secciones; la optimizacion fue interna y de estabilidad.

Archivos tocados hoy:

- `apps/web/src/pages/AdminDashboardPage.tsx`
- `apps/web/src/pages/AssemblyPreparationPage.tsx`
- `apps/web/src/pages/UnitsListPage.tsx`
- `apps/web/src/components/PlatformShell.tsx`
- `apps/web/src/components/Icon.tsx`
- `apps/web/src/App.tsx`
- `apps/web/src/styles.css`

Verificaciones ejecutadas:

- `npm --workspace @ph/web run typecheck`
- `npm --workspace @ph/web run build`
- `http://127.0.0.1:5173/dashboard` respondio `200`
- `http://127.0.0.1:5173/asamblea/preparacion?step=4` respondio `200`
- rutas filtradas de unidades respondieron `200`

Datos para levantar/cargar la pagina facilmente:

- URL principal del dashboard: `http://127.0.0.1:5173/dashboard`
- URL de API healthcheck: `http://127.0.0.1:4000/health`
- API:
  - script: `npm --workspace @ph/api run dev`
  - puerto por defecto: `4000`
  - config: `apps/api/src/config.ts` usa `PORT ?? 4000` y `HOST ?? "0.0.0.0"`
- Frontend:
  - script recomendado: `npm --workspace @ph/web run dev -- --host 127.0.0.1 --port 5173 --strictPort`
  - URL esperada: `http://127.0.0.1:5173/dashboard`
- Problema recurrente:
  - Vite/esbuild a veces falla con `spawn EPERM` dentro del sandbox.
  - Si pasa, arrancar el frontend con permisos elevados usando el mismo comando:
    `npm --workspace @ph/web run dev -- --host 127.0.0.1 --port 5173 --strictPort`
  - Si se necesita dejarlo en background en Windows:
    `Start-Process -FilePath 'C:\Program Files\nodejs\npm.cmd' -ArgumentList '--workspace','@ph/web','run','dev','--','--host','127.0.0.1','--port','5173','--strictPort' -WorkingDirectory 'c:\Users\user\.openclaw\workspace\projects\propiedad-horizontal-app'`
- Orden de diagnostico cuando el usuario diga que la pagina no carga:
  1. Probar `http://127.0.0.1:4000/health`.
  2. Probar `http://127.0.0.1:5173/dashboard`.
  3. Si API responde y web no, levantar solo frontend.
  4. Si Vite muestra `spawn EPERM`, reintentar con permisos elevados.
  5. Confirmar de nuevo `/dashboard` con `200`.

Punto exacto para continuar manana:

- Revisar visualmente el dashboard completo despues de las optimizaciones, especialmente:
  - footer final del dashboard
  - previews/accesos directos inferiores
  - alertas criticas y filtro exacto por `unitIds`
  - checklist + cronometro de preparacion
- No recortar secciones salvo que el usuario lo pida. El usuario quiere conservar las secciones y optimizar jerarquia, consistencia, responsive y mantenimiento.
- Siguiente paso tecnico recomendado:
  - separar mas componentes del dashboard (`MetricTiles`, `ReadinessPanel`, `AttentionUnitsPanel`, `CriticalAlertsPanel`, `ModulePreviewGrid`) si el archivo sigue creciendo.
  - revisar responsive con ancho de escritorio y movil.
  - eventualmente crear un endpoint agregado `/dashboard-summary` para no pedir tantas fuentes desde el frontend.

Continuacion de la sesion - footer global:

- El usuario detecto que el footer seguia quedando visualmente detras del chat de QuorumIA y que la columna izquierda seguia apareciendo en la franja inferior.
- Se movio la solucion al layout global de `PlatformShell`, no al contenido del dashboard:
  - El footer queda como hijo directo de `.platform-dashboard`, despues de `.platform-workspace`.
  - `.platform-dashboard` usa dos filas (`auto auto`) y el footer ocupa `grid-column: 1 / -1`.
  - El footer recibio `z-index: 50`, fondo opaco oscuro y ancho completo para cubrir sidebar y agente cuando se llega al final.
  - Sidebar y workspace quedaron explicitamente en la primera fila del grid.
  - Se elimino el `DashboardFooter` duplicado de `AdminDashboardSections.tsx`.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/dashboard` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion de la sesion - pagina Copropiedad:

- El usuario pidio pasar a la pagina de Copropiedad y aplicar los mismos criterios usados en dashboard: menos bloques gigantes, radios controlados, mejor jerarquia, textos mas legibles y diagramacion mas ordenada.
- Se redisenio `PropertyOverviewPage.tsx`:
  - Header compacto de datos y estructura.
  - Acciones principales alineadas sin mega-botones.
  - Franja de KPIs: unidades, estructura, alistamiento y reglas activas.
  - Ficha de copropiedad en grilla de datos.
  - Reglas operativas como filas claras con estado.
- Se agregaron estilos propios para `.property-overview` en `apps/web/src/styles.css`, incluyendo responsive y modo oscuro.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/copropiedad` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion de la sesion - pagina Unidades y propietarios:

- El usuario pidio aplicar los mismos criterios visuales del dashboard y Copropiedad a `Unidades y propietarios`.
- Se redisenio `UnitsListPage.tsx`:
  - Header compacto con acciones `Agregar unidades` y `Ver copropiedad`.
  - Franja de KPIs: resultados, listas, atencion y votantes.
  - Buscador integrado en un panel de lista.
  - Reemplazo de cards grandes por filas tipo tabla en escritorio.
  - En responsive la tabla se apila para evitar columnas forzadas.
- Se agregaron estilos `.units-*` en `apps/web/src/styles.css`, con soporte de modo oscuro y responsive.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/unidades` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion de la sesion - pagina Asambleas:

- El usuario pidio aplicar los mismos criterios visuales a la pagina `Asambleas`.
- Se redisenio `AssembliesPage.tsx`:
  - Header compacto con estado real de la asamblea.
  - Se agrego carga de `fetchAssemblySettings` para usar la asamblea configurada real y eliminar el link hardcodeado `asm-2026-05`.
  - Franja de KPIs: unidades totales, representadas/quorum, destinatarios y atencion.
  - Flujo de asamblea en filas claras: configuracion base, orden del dia, convocatoria y representacion.
  - Ficha lateral de asamblea actual con fecha, estado, modalidad, base de votacion y servicio.
- Se agregaron estilos `.assemblies-*` en `apps/web/src/styles.css`, con modo oscuro y responsive.
- Verificaciones:
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion de la sesion - pagina Comunicaciones:

- El usuario pidio aplicar los mismos criterios visuales a `Comunicaciones`.
- Se redisenio `CommunicationsPage.tsx`:
  - Header compacto con estado de bandeja (`Requiere revision` / `Sin pendientes`).
  - Acciones hacia representacion y unidades.
  - Franja de KPIs: pendientes, en revision, con archivo y enviadas por tercero.
  - Panel de solicitudes de poder como bandeja de revision, con archivo, motivos, nota y acciones.
  - Estado vacio mas claro cuando no hay poderes pendientes.
- Se agregaron estilos `.communications-*` en `apps/web/src/styles.css`, con responsive y modo oscuro.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/comunicaciones` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion de la sesion - pagina Reportes:

- El usuario pidio aplicar los mismos criterios visuales a `Reportes`.
- Se redisenio `ReportsPage.tsx`:
  - Header compacto con estado (`Con alertas` / `Operacion estable`).
  - Acciones hacia preparacion de asamblea y revision de unidades.
  - Franja de KPIs: unidades totales, quorum proyectado, destinatarios y riesgos abiertos.
  - Riesgos por unidad en filas mas legibles.
  - Panel lateral de indicadores pendientes.
  - Estado vacio cuando no hay riesgos por unidad.
- Se agregaron estilos `.reports-*` en `apps/web/src/styles.css`, con responsive y modo oscuro.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/reportes` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

- Ajuste posterior en Reportes:
  - El usuario pidio que `Riesgos por unidad` se pareciera mas a la seccion del dashboard `Unidades con atencion requerida`.
  - Se actualizo la lista para usar `formatUnitLabel`, mostrar la unidad como etiqueta completa, el representante como texto secundario y el estado compacto a la derecha.
  - Se simplifico `.reports-risk-row` a dos columnas como el patron de `operations-row`.
  - Verificaciones: `npm --workspace @ph/web run typecheck`, `npm --workspace @ph/web run build`, `/reportes` respondio `200`.

Continuacion de la sesion - pagina Configuracion:

- El usuario pidio aplicar el mismo criterio visual a `Configuracion`.
- Se redisenio `SettingsPage.tsx`:
  - Header compacto con chip de perfil, reemplazando el avatar gigante.
  - Franja de KPIs: cuenta, preferencias, opciones y zona horaria.
  - Panel de `Mi cuenta` como lista de datos en vez de inputs de solo lectura.
  - Panel de `Separacion clara` para mantener reglas de negocio fuera de Configuracion.
  - Cards de preferencias con icono, titulo, descripcion y opciones preparadas.
- Se agregaron estilos `.settings-*` en `apps/web/src/styles.css`, con responsive y modo oscuro.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/configuracion` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Continuacion de la sesion:

- Se hizo un refactor de mantenimiento del dashboard sin cambiar comportamiento ni quitar secciones.
- Se creo `apps/web/src/components/dashboard/AdminDashboardSections.tsx` para aislar secciones visuales:
  - `DashboardCommandCenter`
  - `MetricTileGrid`
  - `AssemblyReadinessPanel`
  - `AttentionUnitsPanel`
  - `CriticalAlertsPanel`
  - `DashboardModuleGrid`
  - `DashboardFooter`
- `AdminDashboardPage.tsx` quedo mas enfocado en carga de datos, calculos y composicion.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - API `http://127.0.0.1:4000/health` respondio `200`
  - Web `http://127.0.0.1:5173/dashboard` respondio `200`

Configuracion:

- El usuario no estaba convencido con los botones de `Configuracion` y `Cerrar sesion` del sidebar.
- Se ajusto el footer del sidebar:
  - `Configuracion` ahora se ve como item de navegacion con subtitulo `Cuenta y preferencias`.
  - `Cerrar sesion` quedo como accion secundaria discreta con icono.
- El usuario aprobo esa version.
- Se acordo que `Configuracion` no debe competir con Copropiedad, Asambleas o Comunicaciones.
- Se creo la ruta/pagina `/configuracion` como `Cuenta y preferencias`, enfocada en:
  - Mi cuenta
  - Apariencia
  - Notificaciones
  - Seguridad
  - Facturacion y plan
  - Integraciones
- La pagina aclara que reglas de negocio viven fuera de Configuracion.
- Archivos tocados:
  - `apps/web/src/pages/SettingsPage.tsx`
  - `apps/web/src/App.tsx`
  - `apps/web/src/components/PlatformShell.tsx`
  - `apps/web/src/components/Icon.tsx`
  - `apps/web/src/styles.css`
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/configuracion` respondio `200`

Asistente Quorum fase 1:

- Se acordo implementar una fase 1 del agente como diferencial de plataforma, con escalamiento posterior a IA real, actas, audio/transcripcion y envios.
- Se implemento un asistente persistente en `PlatformShell`:
  - boton `Asistente` en la topbar
  - drawer lateral derecho
  - contexto activo
  - sugerencias rapidas
  - historial corto de mensajes
  - input para preguntas
- Por ahora el motor es local/reglado, sin consumo de tokens ni llamadas a OpenAI.
- En dashboard se pasa contexto real al asistente:
  - estado operativo
  - preparacion
  - quorum proyectado
  - unidades representadas
  - poderes pendientes
  - unidades sin votante
  - documentos cargados
  - convocatorias enviadas
- Archivos tocados:
  - `apps/web/src/components/PlatformShell.tsx`
  - `apps/web/src/components/Icon.tsx`
  - `apps/web/src/pages/AdminDashboardPage.tsx`
  - `apps/web/src/styles.css`
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/dashboard` respondio `200`
  - `http://127.0.0.1:5173/configuracion` respondio `200`

Repaso visual de toda la plataforma:

- El usuario pidio aplicar los mismos criterios visuales en toda la plataforma.
- Se ajusto una capa compartida en `apps/web/src/styles.css` para reducir escala y ordenar jerarquia:
  - tipografia base mas limpia con stack tipo Inter/system UI
  - botones mas compactos y con sombras menos pesadas
  - headers de paginas mas contenidos
  - KPIs y metric tiles con menor altura y numeros menos enormes
  - tarjetas, filas, wizards, modulos y owner details con padding mas sobrio
  - dashboard hero, readiness ring y module previews mas compactos
  - footer global con menor altura pero manteniendo ancho completo
- Se verifico:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - rutas web `200`: `/dashboard`, `/copropiedad`, `/unidades`, `/asambleas`, `/comunicaciones`, `/reportes`, `/configuracion`, `/asamblea/preparacion`

Correccion puntual de `/asamblea/preparacion`:

- El usuario noto que esa pagina aun no tenia los criterios visuales aplicados.
- Se agrego `assembly-preparation-page` a `AssemblyPreparationPage.tsx`.
- Se agregaron reglas especificas en `styles.css` para:
  - header de preparacion mas compacto
  - resumen superior mas sobrio
  - wizard de 7 pasos mas legible y responsive
  - panel principal + rail lateral menos pesados
  - tarjetas/listas/formularios de agenda, documentos, convocatoria, acceso y revision final mas compactos
  - comportamiento responsive en tablet y movil
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asamblea/preparacion` respondio `200`

Rediagramacion de cabina de preparacion:

- El usuario pidio revisar a detalle `/asamblea/preparacion` porque es la seccion principal de la plataforma.
- Se cambio la intencion visual de formulario largo a centro de mando:
  - hero operativo con estado, titulo de asamblea, acciones para continuar, abrir sala e ir al historico
  - tarjeta de asamblea activa con fecha, estado, modalidad, documentos y convocatorias
  - resumen operativo y siguiente enfoque con acceso a historico
  - banco de trabajo con wizard de pasos, estado por paso y rail contextual
- Se agregaron clases CSS:
  - `assembly-prep-hero`
  - `assembly-prep-overview`
  - `assembly-prep-focus`
  - `assembly-history-card`
  - `assembly-prep-workbench`
  - estados visuales `assembly-step-*`
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asamblea/preparacion` respondio `200`
  - `http://127.0.0.1:5173/asambleas` respondio `200`

Reset de datos de asamblea para prueba completa:

- El usuario pidio eliminar los datos que habia puesto en la asamblea programada para probar el flujo completo desde cero.
- Se creo respaldo local: `apps/api/data/app-db.before-assembly-flow-reset-20260412-190213.json`.
- Se limpiaron solo datos de asamblea en `apps/api/data/app-db.json`:
  - `assemblies`: 1 -> 0
  - `agendaItems`: 3 -> 0
  - `assemblyDocuments`: 3 -> 0
  - `assemblyInvitations`: 90 -> 0
  - `assemblyAccessConfigs`: 1 -> 0
  - `assemblyAccessGrants`: 100 -> 0
- Se conservaron copropiedad, unidades, propietarios y poderes/datos de propietarios.
- Verificaciones:
  - `http://127.0.0.1:5173/asamblea/preparacion` respondio `200`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Correccion de Internal Server Error tras reset:

- El usuario reporto `Internal server error` en la pagina.
- Causa encontrada: al limpiar `apps/api/data/app-db.json`, PowerShell lo guardo con BOM UTF-8; `JSON.parse` del servidor fallaba con `Unexpected token '﻿'`.
- Se reescribio `app-db.json` como UTF-8 sin BOM.
- Para diagnosticar se creo una sesion temporal local, se probaron los endpoints de asamblea con token y luego se elimino esa sesion y el archivo temporal del token.
- Verificaciones:
  - endpoints `assembly-readiness`, `assembly-settings`, `assembly-agenda`, `assembly-documents`, `assembly-invitations`, `assembly-access` respondieron `200`
  - `http://127.0.0.1:5173/asamblea/preparacion` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`
  - primeros bytes de `app-db.json`: `7B 0D 0A` (sin BOM)

Simplificacion de Asambleas vs Preparar Asamblea:

- El usuario noto que `/asambleas` competia con `/asamblea/preparacion` y habia demasiada informacion.
- Se redefinio:
  - `/asambleas`: indice ligero con asamblea activa e historico, sin KPIs ni flujo operativo.
  - `/asamblea/preparacion`: cabina operativa para ejecutar el checklist y preparar la asamblea.
- Se quitaron de `AssemblyPreparationPage` los bloques duplicados de ciclo completo e historico.
- Se agrego layout `assemblies-index-layout` y estado vacio sencillo para `/asambleas`.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`

Compactacion fuerte de `/asambleas`:

- El usuario indico que habia demasiado espacio perdido y pidio mejorar UI/UX.
- Se compacto:
  - header de 150px a banda de 104px aprox.
  - gaps generales de la pagina
  - layout principal a columnas mas densas
  - estado vacio de asamblea como fila accionable con boton interno
  - metadatos de asamblea activa como banda horizontal de 4 columnas
  - historico como bloque bajo de consulta, sin card alta vacia
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:5173/asamblea/preparacion` respondio `200`

Ajuste visual de `/asambleas`:

- El usuario mostro captura donde `/asambleas` se veia cruda y con texto quebrado en columnas angostas.
- Se ajusto el header para ser mas compacto y alineado.
- Se cambio `assemblies-index-layout` a columnas mejor balanceadas.
- Las cards de asamblea activa e historico ahora tienen titulos sin columna de icono fantasma (`assemblies-section-title-plain`).
- La lista de datos de asamblea activa usa dos columnas internas; el estado vacio y el historico tienen alturas menores y mejor wrapping.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`

Aplicacion del flujo logico de Asamblea:

- El usuario pidio implementar el orden logico explicado para la seccion de asamblea.
- Se agrego en `/asamblea/preparacion` una navegacion de ciclo completo:
  - Centro de asambleas
  - Preparacion
  - Convocatoria
  - Sala en vivo
  - Cierre e historico
- El boton `Continuar preparacion` ahora lleva al primer paso pendiente del checklist.
- El wizard de preparacion muestra estado por paso.
- En `/asambleas` se agrego bloque `Historico y cierres` preparado para actas, resultados y trazabilidad; por ahora muestra placeholder honesto hasta que exista API/listado de asambleas cerradas.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asamblea/preparacion` respondio `200`
  - `http://127.0.0.1:5173/asambleas` respondio `200`

Persistencia de shell en `/asamblea/preparacion`:

- El usuario noto que no aparecian sidebar, topbar ni QuorumIA.
- Se corrigio `AssemblyPreparationPage.tsx` para usar `PlatformShell` con `activeSection="assemblies"`.
- Se agrego estado `admin` desde `fetchMe()` y contexto propio para QuorumIA en preparacion de asamblea.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asamblea/preparacion` respondio `200`

Reduccion de carga cognitiva en `/asambleas`:

- El usuario indico que el area de asamblea tenia demasiada informacion y necesitaba un orden muy intuitivo.
- Se volvio a simplificar `AssembliesPage.tsx` para que `/asambleas` funcione como indice compacto:
  - cabecera minima con accion principal
  - una fila de trabajo para asamblea activa
  - una fila de consulta para historico
  - sin hero grande ni cards altas que compitan con `/asamblea/preparacion`
- Se agregaron estilos `assemblies-index-head`, `assemblies-worklist`, `assemblies-worklist-row`, `assemblies-row-actions` y `assemblies-history-inline`.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Rediagramacion visual de `/asambleas`:

- El usuario mostro que la version compacta quedo carente de diseno y diagramacion, con bloques muy vacios.
- Causa tecnica detectada: `.platform-content` ocupaba una fila disponible del layout y las secciones de `/asambleas` se estiraban verticalmente; se corrigio con `align-content: start` en `.assemblies-overview`.
- Se redisenio la composicion:
  - cabecera compacta con tarjeta de siguiente paso
  - panel principal `assembly-active-board`
  - guia de preparacion en 4 pasos compactos
  - panel lateral `assemblies-history-card` para historico sin competir con la cabina de preparacion
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Coherencia de pasos de preparacion:

- El usuario pidio que los pasos visibles en `/asambleas` fueran coherentes con los del dashboard.
- Se reemplazo la guia resumida de 4 pasos por los mismos 7 pasos del dashboard y de `/asamblea/preparacion`:
  - Tipo y datos base de la asamblea
  - Orden del dia estructurado
  - Documentos y anexos
  - Representacion y poderes
  - Convocatoria y destinatarios
  - Acceso e identidad
  - Revision final de alistamiento
- Cada paso en `/asambleas` enlaza al mismo `?step=N` de la cabina de preparacion.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:5173/asamblea/preparacion?step=6` respondio `200`

Interaccion por pasos en `/asambleas`:

- El usuario pidio quitar la seccion `Siguiente paso`, quitar el boton redundante de preparar y poner todos los pasos en un solo renglon.
- Se convirtio la ruta de preparacion en tabs horizontales de 7 pasos:
  - al hacer click en un paso, se despliega abajo su panel de configuracion/resumen
  - cada paso enlaza desde el panel a `/asamblea/preparacion?step=N` para editar
  - los pasos completos cambian de color y muestran check
  - el estado completo sale de datos reales: configuracion de asamblea, agenda, documentos, poderes/unidades, convocatorias y accesos
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:5173/asamblea/preparacion?step=0` respondio `200`

Ajuste de edicion inline en `/asambleas`:

- El usuario aclaro que el boton `Editar paso` no debe redireccionar; todo debe ocurrir en la misma pagina.
- Se elimino el boton `Editar paso` y el cuadro visual de estado `Pendiente/Completado` del panel inferior.
- Los 7 recuadros funcionan como botones/tabs; el paso 01 queda desplegado por defecto al cargar.
- El panel inferior ahora muestra la ficha/resumen de configuracion del paso seleccionado dentro de la misma pagina.
- Se limpio el CSS muerto de `assemblies-next-card`.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`

Guardado inline por paso en `/asambleas`:

- El usuario pidio que el administrador pudiera editar y guardar la informacion de cada paso en la misma pagina.
- Se agrego edicion inline en `AssembliesPage.tsx`:
  - paso 1: configuracion base de asamblea, usando `saveAssemblySettings`
  - paso 2: orden del dia, usando `saveAssemblyAgenda`
  - paso 3: documentos, usando `saveAssemblyDocuments`
  - paso 4: representacion y poderes, refresca/revalida readiness porque la fuente real son unidades/poderes
  - paso 5: convocatoria, usando `saveAssemblyInvitations`
  - paso 6: acceso e identidad, usando `saveAssemblyAccess`
  - paso 7: revision final, refresca readiness, invitaciones y accesos
- Se agrego feedback de guardado/error en la misma pantalla y estilos compactos `assemblies-step-editor`, `assemblies-editor-row`, `assemblies-save-step`.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
  - `http://127.0.0.1:4000/health` respondio `200`

Mejor diagramacion de despliegues por paso:

- El usuario pidio una diagramacion mucho mejor para cada despliegue de los pasos.
- Se ajusto el panel desplegado:
  - intro lateral del paso con numero, titulo, explicacion y estado contextual
  - formulario a la derecha en grilla compacta
  - filas internas para agenda/documentos/convocatoria con alineacion consistente
  - boton de guardado fuera de las celdas de campos, alineado al cierre del formulario
  - mejor fondo, bordes y espaciado para que no parezca tabla improvisada
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`

Historico debajo del flujo en `/asambleas`:

- El usuario pidio mover la caja de Historico para dar mas espacio a los campos de cada paso.
- Se saco `assemblies-history-card` de la grilla lateral y se movio a una seccion inferior `assemblies-history-section`.
- `assemblies-command-grid` ahora usa una sola columna para que `assembly-active-board` ocupe todo el ancho disponible.
- El historico quedo como banda inferior horizontal y responsive.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`

Correccion de ruptura visual en `/asambleas`:

- El usuario reporto que la pagina quedo "rotisima" despues de mover Historico.
- Causa: el selector combinado `.assembly-active-board, .assemblies-history-card` recibio la grilla de 3 columnas pensada solo para Historico, aplastando el panel operativo principal.
- Se separaron las reglas:
  - `assembly-active-board` vuelve a una sola columna y usa todo el ancho.
  - `assemblies-history-card` conserva la grilla horizontal inferior.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`

Alineacion de pasos y boton de guardado:

- El usuario pidio alinear las descripciones de los 7 pasos y fijar el boton de guardado siempre abajo a la derecha.
- Se ajusto `.assemblies-prep-step` con filas internas fijas para numero, titulo y descripcion, evitando que textos de distinta longitud muevan el contenido.
- Se anclo `.assemblies-save-step` con posicion absoluta dentro del formulario del paso, con padding inferior reservado en `.assemblies-step-config`.
- Verificaciones:
  - `npm --workspace @ph/web run typecheck`
  - `npm --workspace @ph/web run build`
  - `http://127.0.0.1:5173/asambleas` respondio `200`
